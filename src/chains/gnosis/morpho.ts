import { MorphoToken } from '../../types';

/**
 * Gnosis Morpho Blue markets — none discovered in Phase 0 (MND-1107 / MND-1121).
 * Morpho Blue + IRM factories are live; populate market IDs after RPC discovery
 * then publish a tokenlist bump.
 */
export const tokens: MorphoToken[] = [];
